source :rubygems

# Optionally configure bundler to use additional options when installing chef:
# bundle config build.chef --no-ri --no-rdoc
gem "chef"
gem "knife-solo"
gem "librarian"
# Vagrant already installed from Package as recommended by Vagrant docs, but could be installed as gem
# gem "vagrant"
